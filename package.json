{
  "name": "mentornet-monorepo",
  "private": true,
  "scripts": {
    "web": "pnpm --filter @mentornet/web dev",
    "mobile": "pnpm --filter @mentornet/mobile start",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "prepare": "husky"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
